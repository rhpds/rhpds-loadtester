---
# No handlers needed — SSH config and key are written directly to
# /opt/showroom/.ssh/ on the bastion host and mounted into the container.
# Changes take effect immediately without any container restart.
